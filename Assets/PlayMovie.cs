using UnityEngine;
using System.Collections;

public class PlayMovie : MonoBehaviour {

	// Use this for initialization
	void Start () {
       // MovieTexture panorama = (MovieTexture)GetComponent<Renderer>().material.mainTexture;
        //panorama.loop = true;
        //panorama.Play();
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
